using HelixToolkit.Wpf;

namespace GeoModeler3D.Rendering;

/// <summary>
/// Integrates HelixToolkit cutting plane clipping groups.
/// Stub — to be implemented in a future phase.
/// </summary>
public class CuttingPlaneVisualizer
{
    private HelixViewport3D? _viewport;

    public void Initialize(HelixViewport3D viewport)
    {
        _viewport = viewport;
    }
}
